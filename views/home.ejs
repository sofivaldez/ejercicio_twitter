<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-3" id="col-sidebar"><%- include ("partials/sidebar") %></div>
        <div class="col-6">
          <div><a href="/profile/<%= loggedUser.username %>">
            <img
              id="avatar-home"
              src="/img/<%= loggedUser.avatar %>"
              alt="avatar"
              height="150px"
              width="150px"
          /></a>
          <form action="/tweet" method="post">
            <div class="mb-3">
              <textarea
                name="tweet"
                class="form-control"
                id="bio"
                rows="3"
                placeholder="¿Qué está pasando?"
              ></textarea>
              <button id="btn-tweet" type="submit" class="btn btn-primary rounded-pill mt-3">
                Tweet
              </button></div>
          
            </div>
          </form>
          <h5>Tus tweets:</h5>
          <% for(const tweet of ownTweets) { %>
          <div class="mt-3 mb-3">
            <p><%= tweet.content %></p>
            <p>a <%= tweet.createdAt %></p>
          </div>

          <a href="/deleteTweet/<%= tweet.id %> "
            ><button class="btn btn-danger">Eliminar</button></a
          >
          <% if (!tweet.likes.includes(loggedUser.id)) { %>
          <a href="/like/<%= tweet._id %> "
            ><button class="btn btn-outline-primary">Me gusta</button></a
          >
          <span style="color: blue"><%= tweet.likes.length %> </span>
          <% } else { %>
          <a href="/like/<%= tweet._id %> "><button class="btn btn-primary">Me gusta</button></a>
          <span style="color: blue"><%= tweet.likes.length %> </span>
          <% } %>
          <hr />
          <% } %>

          <h5>
            Hola <%= loggedUser.firstname %>! Te extrañamos! Echa un vistazo a lo que han publicado
            tus cuentas de interés:
          </h5>
          <% for(const tweet of wantedTweets) { %>
          <a href="profile/<%= tweet.user.username %> ">
            <img src="/img/<%= tweet.user.avatar %>" alt="avatar" height="80px" width="80px"
          /></a>
          <a href="/unfollow/<%= tweet.user.username %>" class="btn btn-dark rounded-pill">Dejar de seguir</a>
          <h6><%= tweet.user.username %> publicó:</h6>
          <p><%= tweet.content %></p>
          <p>a <%= tweet.createdAt %></p>
          <% if (!tweet.likes.includes(loggedUser.id)) { %>
          <a style="color: rgb(255, 106, 196)" href="/like/<%= tweet._id %> "
            ><i class="far fa-heart"></i></a
          >
          <span style="color: rgb(255, 106, 196)"><%= tweet.likes.length %> </span>
          <% } else { %>
          <a style="color: rgb(255, 106, 196)" href="/like/<%= tweet._id %> "><i class="fas fa-heart"></i></a>
          <span style="color: rgb(255, 106, 196)"><%= tweet.likes.length %> </span>
          <% } %>
          <hr />
          <% } %>
        </div>
        <div class="col-3">
          <div id="whoToFollow"> 
            <h5>Cuentas a seguir:</h5>
            <div class="d-flex">
              <% for(const recommendedUser of recommendedUsers) { %>
                <div class="mb-3">
                  <a href="profile/<%= recommendedUser.username %> ">
                    <img
                    id="avatar-colleft"
                      src="/img/<%= recommendedUser.avatar %> "
                      alt="avatar"
                      width="80px"
                      height="80px"
                    />
                    <h6><%= recommendedUser.username %></h6>
                  </a>
               </div>
               <div>
                <a href="follow/<%= recommendedUser.username %> "
                  ><button class="btn btn-dark rounded-pill">Seguir</button></a
                >
               </div>
            </div>
            <% } %></div>
         
        </div>
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
