<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <form action="/tweet" method="post">
      <div class="mb-3">
        <textarea
          name="tweet"
          class="form-control"
          id="bio"
          rows="3"
          placeholder="¿Qué está pasando?"
        ></textarea>
        <button type="submit" class="btn btn-primary">Tweet</button>
      </div>
    </form>
    <h6>
      Hola <%= loggedUser.firstname %>! Te extrañamos! Echa un vistazo a lo que han publicado tus
      cuentas de interés:
    </h6>
    <% for(const tweet of wantedTweets) { %>
    <img src="/img/<%= tweet.user.avatar %>" alt="avatar" height="80px" width="80px" />
    <p><%= tweet.user.username %> publicó:</p>
    <p><%= tweet.content %></p>
    <p>a <%= tweet.createdAt %></p>
    <a href="/profile/<%= tweet.user.username %>" class="btn btn-primary">Ver perfil</a>
    <hr />
    <% } %>
    <a href="/profile/<%= loggedUser.username %>" class="btn btn-primary">Tu perfil</a>
    <h6>Cuentas a seguir:</h6>
    <% for(const recommendedUser of recommendedUsers) { %>
    <img src="/img/<%= recommendedUser.avatar %> " alt="avatar" width="100px" height="100px" />
    <% } %>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
