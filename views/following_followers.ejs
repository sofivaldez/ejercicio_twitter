<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <% if (users.length===0&&role==='following') { %>
    <p>Aún no sigues ninguna cuenta</p>
    <% } %> <% if (users.length===0&&role==='followers') { %>
    <p>Aún no tienes seguidores</p>
    <% } %> <% if (users.length!==0) { %> <% for( const user of users ) { %>
    <div class="mb-3">
      <a href="/profile/<%= user.username %> ">
        <img src="/img/<%= user.avatar %> " alt="avatar" width="80px" height="80px" />
        <h6><%= user.username %></h6>
      </a>
    </div>
    <% } %> <% } %>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
